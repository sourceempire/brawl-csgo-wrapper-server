/* istanbul ignore file */
/* tslint:disable */
/* eslint-disable */

/**
 * Represents a side (CT vs T). `null` if no side. `Side` in SourceMod and represented as an integer enum.
 */
export enum Get5Side {
    CT = 'ct',
    T = 't',
    SPEC = 'spec',
}
